package com.codedisaster.steamworks;

/**
 * Purpose: called when a new controller has been disconnected, will fire once
 * per controller if multiple new controllers disconnect in the same frame
 */
@SuppressWarnings("WeakerAccess")
public class SteamInputDeviceConnected extends SteamNativeHandle {

	SteamInputDeviceConnected(long handle) {
		super(handle);
	}
	
	//TODO what is this enum
	public enum enummmm{
		
	}

}
